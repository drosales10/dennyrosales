{
  "mcpServers": {
    "EXAMPLE-github": {
      "command": "pnpm",
      "args": [
        "dlx",
        "@anthropic/github-mcp-server"
      ],
      "env": {
        "GITHUB_TOKEN": "${GITHUB_TOKEN}"
      }
    }
  }
}
